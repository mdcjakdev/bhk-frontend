import { StopMonitorDirective } from './stop-monitor.directive';

describe('StopMonitorDirective', () => {
  it('should create an instance', () => {
    const directive = new StopMonitorDirective();
    expect(directive).toBeTruthy();
  });
});
