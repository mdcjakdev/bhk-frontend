import { MatTooltipAsNotificationDirective } from './mat-tooltip-as-notification.directive';

describe('MatTooltipAsNotificationDirective', () => {
  it('should create an instance', () => {
    const directive = new MatTooltipAsNotificationDirective();
    expect(directive).toBeTruthy();
  });
});
